include $(top_srcdir)/build/common.am

includedir = $(prefix)/include/maia-$(MAIA_ABI)

INCLUDES = \
    $(MAIA_CFLAGS) \
    -I$(top_srcdir)/build \
    -I$(top_srcdir)/src

VALAFLAGS = \
    --debug \
    --vapidir=${top_srcdir}/vapi \
    --vapidir=. \
    --pkg=object \
    --pkg=source \
    --pkg=posix \
    --pkg=vala-0.10 \
    --pkg=gee-1.0 \
    --pkg=pixman-1

