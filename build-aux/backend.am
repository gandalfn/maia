backend_DATA = $(MAIA_LIB_NAME).backend

AM_LDFLAGS = \
    -Wl,--as-needed \
    -avoid-version \
    -no-undefined

EXTRA_DIST += \
    $(MAIA_LIB_NAME).backend.in

CLEANFILES += \
    $(MAIA_LIB_NAME).backend
